apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-sendgrid.fullname" . }}
  labels:
    {{- include "kafka-sendgrid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-sendgrid.selectorLabels" . | nindent 4 }}